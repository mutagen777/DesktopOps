<Window x:Class="DesktopOps.Agent.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        Title="DesktopOps Agent"
        Height="0"
        Width="0"
        WindowStyle="None"
        ShowInTaskbar="False"
        Visibility="Hidden">
    <Grid>
        <tb:TaskbarIcon x:Name="TrayIcon"
                        ToolTipText="DesktopOps Agent"
                        MenuActivation="RightClick"
                        TrayMouseDoubleClick="OnTrayDoubleClick">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu Opened="OnContextMenuOpened">
                    <MenuItem Header="Konfiguration" Click="OnConfigurationClick" />
                    <Separator />
                    <MenuItem Header="Updates suchen..." Click="OnSearchUpdatesClick" />
                    <MenuItem x:Name="LastSearchMenuItem" Header="Letzte Suche: –" IsEnabled="False" />
                    <MenuItem Header="Programme neuinstallieren" Click="OnReinstallClick" />
                    <Separator />
                    <MenuItem Header="Diagnose exportieren" Click="OnExportDiagnosticsClick" />
                    <MenuItem x:Name="VersionMenuItem" Header="Version: –" IsEnabled="False" />
                    <Separator />
                    <MenuItem Header="Beenden" Click="OnExitClick" />
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
    </Grid>
</Window>
