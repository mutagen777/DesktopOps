<Window x:Class="DesktopOps.Agent.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DesktopOps – Updates"
        Width="520"
        Height="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip"
        MinWidth="420"
        MinHeight="320"
        Topmost="True">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="PendingHeader" FontWeight="SemiBold" Margin="0,0,0,8" />

        <ListBox x:Name="ActionList" Grid.Row="1" Margin="0,0,0,12" />

        <TextBlock x:Name="StepText" Grid.Row="2" Margin="0,0,0,6" TextWrapping="Wrap" />
        <ProgressBar x:Name="Progress" Grid.Row="3" Height="18" Minimum="0" Maximum="1" Margin="0,0,0,12" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="ExecuteButton" Width="110" Margin="0,0,8,0" IsDefault="True" Click="OnExecuteClick" />
            <Button x:Name="CloseButton" Width="110" IsCancel="True" Click="OnCloseClick" />
        </StackPanel>
    </Grid>
</Window>
